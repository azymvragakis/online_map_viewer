name: seismic-viewer
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - geopandas>=0.14.0
  - pandas>=2.0.0
  - numpy>=1.24.0
  - shapely>=2.0.0
  - fiona>=1.9.0
  - pyproj>=3.6.0
  - matplotlib>=3.7.0
  - pip
  - pip:
    - flask  # Optional: if using run_server.py alternative
